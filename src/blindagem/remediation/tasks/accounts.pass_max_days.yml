- name: Expire passwords at least once a year for new accounts
  ansible.builtin.lineinfile:
    path: /etc/login.defs
    regexp: '^\s*PASS_MAX_DAYS\b'
    line: "PASS_MAX_DAYS\t365"
    state: present
  tags: [accounts.pass_max_days, accounts]
